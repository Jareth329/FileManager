extends ItemList

# icon_scale can be reverted to 1.0 if I add a second itemlist as an overlay that only shows selected images (to avoid
#	needing the extra space around thumbnails that icon_scale=0.8 provides)
